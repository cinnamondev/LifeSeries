TODO: adapt tasks away from assignment builder structure
to a list of candidate tasks which is initialized from the task deck.

players can then be assigned to a task by simply adding them to the tasks 'player list'. 

should be better for future as it allows the task to maintain their own assignment limit, reduces number of object / 
listeners that may be redundant, makes it easier to develop collaborative tasking. (i.e. all players assigned to task A 
must do this).
